<div class="card-trip">
  <img src="<%= flat.image_url %>" />
  <div class="card-trip-infos">
    <div>
      <h2><%= link_to flat.title, flat_path(flat) %></h2>
      <p><%= flat.description %></p>
    </div>
    <h2 class="card-trip-pricing"><%= flat.price %> €/night</h2>
    <%= image_tag 'placeholder.png', class: "card-trip-user avatar-bordered" %>
  </div>
</div>
